from setuptools import setup, find_packages

setup(
    name="HyQD-electromagnetic-fields",
    version="0.0.0",
    packages=find_packages(),
    install_requires=["numpy"],
    include_package_data=True,
)